export class ProductItem {
    productName: string;
    productCount: Number;
    constructor(productName: string, productCount: number) {
        this.productName = productName;
        this.productCount = productCount;
    }
}